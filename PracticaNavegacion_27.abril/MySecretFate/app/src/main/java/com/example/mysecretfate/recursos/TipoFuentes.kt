package com.example.mysecretfate.recursos

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.mysecretfate.R

val FuentePixel = FontFamily(
    Font(R.font.press_start_2p)
)

// 'Font' es una clase que representa una fuente tipográfica específica.
// 'FontFamily' es una colección de fuentes que se pueden usar en la aplicación.
// En este caso, 'FuentePixel' es una familia de fuentes que contiene una sola fuente,
// 'press_start_2p', que se encuentra en los recursos de la aplicación (en la carpeta 'res/font').